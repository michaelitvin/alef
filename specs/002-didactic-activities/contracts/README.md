# Contracts

This is a frontend-only application with no backend API.

All data is stored in:
- Static YAML files (`src/data/*.yaml`)
- Client-side localStorage via Zustand persist

No API contracts required.
